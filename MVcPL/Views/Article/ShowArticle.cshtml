@model MVcPL.Models.ArticleViewModel.ArticleViewModel
<div id="article">
    <div id="title">

        <h2>@Html.ActionLink(@Model.Title, "ViewArticle", new { controller = "Article", ArticleId = @Model.Id })</h2>

        <div id="info">
            <span id="date">@Model.PublicationDate</span>
            <span id="user">
                <img src="/Content/images/icon_user_gray.png" />
                @Html.ActionLink(@Model.User.UserName, "ViewBloggerArticles", new { controller = "Article", bloggerid = @Model.UserId }, new { @title = "User's blog" })
            </span>

            @*@if (HttpContext.Current.User.IsInRole("admin") || (Request.IsAuthenticated && Convert.ToInt32(Profile.GetPropertyValue("Id")) == Model.Authorid))
                {
                <span class="pull-right"><img src="/Content/images/icon_edit.png" /> @Html.ActionLink("Edit", "UpdateArticle", new { controller = "Article", ArticleId = @Model.Id })
                    <img src="/Content/images/icon_delete.png" /> @Html.ActionLink("Delete", "DeleteArticle", new { controller = "Article", ArticleId = @Model.Id })
                </span>
                }*@

        </div>
    </div>
    <div id="content">@Model.Content</div>
    <div id="separate">***</div>
</div>
@*<div id="separate">***</div>*@
